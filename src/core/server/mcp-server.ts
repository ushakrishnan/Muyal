// Legacy re-export removed during refactor. The canonical MCP server implementation lives under src/core/server/unified-agent-server.ts or the new server module.
// If a specific export is required here, re-export it explicitly from the canonical file.
